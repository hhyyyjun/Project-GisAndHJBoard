<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
                        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lee.mapstudy.boardDao.MemberDao">

<insert id="insertM" parameterType="CustomMap">
	INSERT INTO TB_MEMBER(MID, MPW, MNICK, MROLE)
	VALUES(#{id}, #{pw}, #{nick}, #{role})
</insert>

<select id="selectOne" parameterType="CustomMap" resultType="CustomMap">
	SELECT MID 
	      ,MPW <!--  -->
	      ,MNICK
	      ,MROLE
	  FROM TB_MEMBER 
	 WHERE 1 = 1
	   
	   AND MID = #{id}
	    
	   AND MPW = #{pw}
</select>


<select id="checkId" parameterType="String" resultType="int">
	SELECT COUNT(*) as cnt 
	  FROM TB_MEMBER 
	 WHERE 1 = 1
	 
	   AND MID = #{id}
</select>



</mapper>